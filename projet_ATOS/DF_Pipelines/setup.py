import setuptools

setuptools.setup(
    name='custom_read_file',
    version='1.0',
    install_requires=[],
    packages=setuptools.find_packages(),
)


setuptools.setup(
    name='pipeline_commons',
    version='1.0',
    install_requires=[],
    packages=setuptools.find_packages(),
)
